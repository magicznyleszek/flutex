/**
 * Stub for Parcel's `bundle-text:` imports, which Jest cannot resolve. Real markup, so
 * the SSR smoke test still gets a well-formed document.
 */
const markup = '<svg viewBox="0 0 1 1" xmlns="http://www.w3.org/2000/svg"><path d="M0 0h1v1H0z"/></svg>'

export default markup
